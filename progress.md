# 当前进度

## 当前进度快照（2026-08-11）

- 当前基线：`master`；P6-1 评估 PR #12 和独立 E2E 修复 PR #13 均已合并。
- P6-1 技术证据、20×10 次高德技术样本、10/10 真实运营偏差样本及脱敏复核均已完成。
- 当前阶段：P6-1 已完成人工审阅并正式收口；P6-2 未启动。

### P5 阶段总结

- 统计口径为真实试运行订单，排除历史模拟验收单。
- 累计真实订单 8 笔：首日整改前 2 笔均按“乘客未到”异常关闭；整改后 6 笔全部完成。
- 自动派单 6 笔，人工复核 2 笔；两笔人工复核均确认后成功插入既有任务。
- 整改后样本完成率 6/6，不可服务 0，重复资源占用 0；已验证合乘、任务节点和审计闭环。
- P5 结论：整改后运行稳定，用户确认结束 P5；首日误操作样本与稳定样本分开保留。

### P6-1 当前状态

- 真实高德 Web Service Key 的驾车路径和地理编码连通性预检通过，未出现权限、配额或限流错误。
- 已从通渭试点事实库只读提取 31 个有效虚拟站点，完成 48 组候选预分类并固定 20 组路线。
- 正式技术样本为 20 组路线 × 10 次，共 200 次；200/200 成功，成功率 100%，高于 99% 验收阈值。
- 20 组路线距离均无变化；S03 第 10 次 ETA 相对首次正式样本变化 31.71%，绝对变化 13 秒，保留为告警，不判定为接口失败。
- 20/20 路线摘要均有有效路径点和唯一 SHA-256 指纹；整改后 API 全量 265 项、算法 11 项和管理端 184 项单元测试均通过。
- 已从 2026-08-09 试运行完成单中核验并纳入 10/10 笔真实运营偏差样本；全部使用 `AMAP` 且未降级，实际时间可追溯到不可变位置事件，记录延迟不超过 120 秒。
- 10 笔初步统计：上车点绝对误差中位数 300 秒、P90 660 秒；行程绝对误差中位数 557 秒、P90 1214 秒；行程 MAPE 207.6181%；上车点与行程方向均为高估 5 笔、低估 5 笔。
- 技术评估结论已固化到 `docs/pilot/evidence/p6-1-real-route-eta-evaluation-2026-08-09.md`。
- 2026-08-11 人工审阅决定：暂不将 ETA 升级为运营指标，继续标注“仅作参考”。MAPE 207.6% 主要被 2–3 分钟短行程放大，P90 行程绝对误差 1214 秒（约 20.2 分钟），不足以支撑绩效、拒单或硬性派单考核。
- PR #12 审阅通过并合并至 `master`，合并提交为 `f08fe27`；200/200 技术调用成功、10/10 运营样本和脱敏扫描结果均获确认。
- 独立管理端 E2E 修复 PR #13 已合并至 `master`，合并提交为 `0746e26`；类型检查通过、Vitest 184/184、Playwright 5/5。
- P6-1 已正式收口；本结论不自动启动 P6-2。

### 下一步行动

- 继续保留样本纳入门槛：只有同时具备派单时预测 ETA 和可信实际到达时间来源的订单才可用于后续观察；不完整或记录延迟超过 120 秒的样本注明原因并排除。
- ETA 持续标注“仅作参考”，不用于绩效、拒单或硬性派单判断，也不因 10 笔样本完成而自动升级指标。
- P6-2 暂不启动；后续必须先定义具体范围、验收标准和实施计划，再另行评审是否进入执行。

## 试运行批次记录（2026-08-09）

- 当日两批共 6 笔真实订单完成调度和运营闭环，其中 2 笔经人工复核后完成派单，其余自动派单；未发生不可服务或异常关闭。
- 仓库只保留聚合结果，不记录乘客身份，也不建立乘客与订单、任务、车辆、地点或调度评分的关联。

更新时间：2026-08-11（Asia/Shanghai）

## P1 阶段完成

- 阶段状态：P1-1 至 P1-5 已全部完成并通过审阅。
- 当前分支：`codex/p1-vehicle-location-calibration`
- 当前提交：`439228bb45e1f9a9c922f794d626e7f5561d30e0`
- 提交说明：`fix: report algorithm service unavailability`
- 远端状态：提交 `439228b` 已推送到 `origin/codex/p1-vehicle-location-calibration`。

### P1-1 校准 4 辆车首配位置

- 4 辆试点车辆的待命位置已通过后台录入。
- 已核验位置事件、最新快照、位置来源和服务区围栏状态。
- 4 辆车均具备服务区内的有效最新位置，可进入调度候选。

### P1-2 核对车辆可调度状态

- 已只读核对 4 辆车均为 `IDLE`、`dispatchable=true`。
- 已确认车辆均存在最新位置快照，候选车辆数量为 4。

### P1-3 处理历史人工复核订单

- 候选车辆为 0 的历史人工复核决策已完成处理。
- 拒绝或取消原因及对应审计证据已保留。

### P1-4 验证算法服务运行依赖

- 已验证 API 容器内可以访问算法服务 `/health`。
- 已修复算法服务不可用时的错误映射：页面显示“算法服务不可用”，不会误报登录失效。
- 后端全量测试通过；前端类型检查、30 个测试文件共 168 项测试和生产构建通过。

### P1-5 新建订单调度验收

- 测试订单：`7761fa69-98f1-4518-860c-3361fc85716d`
- 已完成一次且仅一次调度，候选车辆数量为 4。
- 最佳候选车辆：`甘J16396D`（`af0d7785-4606-4451-b451-6befa043890a`）。
- 系统生成可确认的人工复核方案，订单状态为 `PENDING_MANUAL_REVIEW`。
- 调度决策和审计记录各 1 条；人工确认前不生成车辆任务。

## 遗留风险

- 当前地图路径能力处于 `STRAIGHT_LINE` 降级状态，人工复核原因为 `MAP_ROUTE_UNAVAILABLE`；真实地图路径和自动派单仍需后续阶段验证。
- P1-5 测试订单仍待运营人员确认或拒绝，当前没有车辆任务；后续操作应保留人工决策原因和审计记录。
- 前端生产构建仍提示单个 JavaScript chunk 超过 500 kB，当前不阻断试点验收，但后续可进行代码拆分优化。
- P1 阶段完成不等同于生产试点批准；运营基础数据、完整人工运营闭环、监控和上线前检查仍需按计划推进。
- `progress.md` 为本地进度文件，当前未纳入 Git 跟踪，也未随 `439228b` 推送。

## P2-1 服务区边界复核

### 已完成的技术核验

- 已确认管理端与 API 运行容器挂载当前 P1 工作树，API 健康状态为 `UP`，实际连接隔离数据库 `drt_ops_pilot_bootstrap`。
- 已只读核对“通渭县试点服务区”处于启用、已发布状态，发布版本为 5，坐标系为 `GCJ02`，边界几何有效。
- 已按系统一致的 `ST_Covers` 口径核对 21 个虚拟站点和 4 辆车最新位置：
  - 21/21 个站点启用、已核验且位于已发布边界内，区外站点为 0。
  - 4/4 辆车最新位置均位于已发布边界内，车辆均为 `IDLE`、`dispatchable=true`。
- 已在真实浏览器调度地图同时启用服务区、虚拟站点和车辆位置图层，核对 25 个标记和 4 个车辆位置卡片；未见明显漂移或边界裁切。
- 已核对服务区创建、保存草稿、发布审计动作链。本次未修改或发布边界。
- 前端资源配置专项测试 3 个文件共 39 项通过；后端最新 Surefire 报告显示服务区命令 11 项、API 7 项通过，PostGIS 条件测试 2 项按默认配置跳过。
- 详细证据：`docs/pilot/evidence/p2-1-service-area-review-2026-07-29.md`。

### 业务确认与状态

- 2026-07-29，用户明确确认现有边界覆盖实际运营范围，并要求收口 P2-1、进入 P2-2。
- P2-1 技术复核和业务边界确认均已完成，当前没有调整边界的依据；本次未创建或发布新草稿。
- 如后续运营范围发生变化，只能基于当前已发布边界创建新草稿并重新发布，不得直接覆盖历史版本。

P2-1 状态：已完成。

## P2-2 补齐并核验虚拟站点

### 已完成的导入与校验

- 当前共有 31 个虚拟站点，达到计划要求的 30 至 50 个。
- 31/31 个站点均具备非空名称和地址、`GCJ-02` 坐标、正数服务半径、上下车能力、核验时间和启停状态；规范化名称无重复。
- 导入审计显示本批首次提交创建 10 条、跳过 0 条；同批再次提交创建 0 条、跳过 10 条，重复导入未生成重复站点。
- 接管页面时本批导入已完成，原始 CSV 未留在授权工作区且页面文件选择框为空；为避免第三次重复提交，本次没有再次点击导入，预校验依据为持久化数据与导入审计。
- 详细证据：`docs/pilot/evidence/p2-2-virtual-stop-import-review-2026-07-29.md`。

### 地图与服务区关系

- 已发布服务区边界仍为 v5，几何有效；按 `ST_Covers` 口径覆盖 26 个站点，5 个站点位于边界外。
- 5 个 v5 区外站点为工商银行、国税局、财政局、工信局、县政府，当前全部停用。
- 数据库已有未发布草稿 v6，几何有效且覆盖 31/31 个站点；真实浏览器资源配置页显示“草稿待发布”、31 个站点、启用 26 个、停用 5 个。
- P2-1 业务确认针对发布版本 v5。v6 扩大运营范围，本次未发布；未经新的业务边界确认不得发布。

### 遗留质量风险与下一步

- 站点安全说明仍为“示例数据，需现场核验”，应在 P2-3 形成启用清单前由运营人员补充或确认。
- P2-3 应形成“启用、暂不启用、待复核”清单；5 个 v5 区外站点保持停用，明确后续是调整站点坐标还是另行审批并发布边界 v6。
- 原始 CSV 未作为验收附件保留，后续批次应归档导入源文件和预校验结果。

P2-2 状态：已完成。下一阶段：P2-3 站点启停与安全说明复核。

## P2-3 站点启用清单确认

- 用户确认 31 个站点统一使用安全说明：“经运营复核，仅在指定安全停靠区域上下客，并服从现场交通管理。”
- 已通过资源配置页面逐项更新 31 个站点；31/31 个安全说明与确认文本一致，旧占位说明为 0。
- 最终站点清单为：启用 31、暂不启用 0、待复核 0。
- `VirtualStopMatcher` 对自动匹配和指定站点匹配均过滤未启用站点，订单匹配只使用已启用站点。

P2-3 状态：已完成。

## P2-4 车辆与驾驶员台账复核

- 用户确认 4 辆车、4 名驾驶员信息有效，联系方式已人工核对。
- 用户确认班次按每日 06:30–19:00 轮班制执行。
- 数据库交叉核验 4 辆车均为空闲且可调度；4 名驾驶员均为 `AVAILABLE`、`QUALIFIED`，联系电话均已登记。

P2-4 状态：已完成。

## P2-5 第二调度员账号验收

- 第二调度员账号为 `dispatcher02`，显示名称为“调度员：dispatcher02”，账号启用且仅具有 `DISPATCHER` 角色。
- 已使用临时凭据完成登录验证；账号要求首次登录改密。
- 订单、车辆任务和车辆位置读取接口返回 HTTP 200；用户管理和规则管理接口被拒绝，并生成 2 条 `AUTHORIZATION_DENIED` 审计。
- `DISPATCHER` 角色具备订单、调度、人工复核、任务和位置相关权限，不具备用户、规则和资源管理权限。

P2-5 状态：已完成。

## P2-6 规则参数复核

- 用户确认当前试点规则：
  - 最大等候 5 分钟、最大绕行 8 分钟、预约窗口 60 分钟。
  - 自动阈值 82、人工阈值 62。
  - 权重 0.35/0.20/0.30/0.15。
  - 插单策略 `REALTIME_INSERTION`。
- 页面和数据库值与上述确认完全一致，规则组处于启用状态。

P2-6 状态：已完成。

## P2 阶段完成

- 原 v5 边界外的 5 个站点由用户改为启用后，管理员已发布覆盖 31 个站点的 v6。
- v6 几何有效，发布边界与草稿一致；31/31 个启用站点位于服务区内，区外启用站点为 0。
- 前端资源、用户、规则专项测试 3 个文件共 12 项通过。
- 后端站点匹配、站点导入、用户管理、规则管理专项测试共 19 项通过，失败 0、错误 0、跳过 0。
- 详细证据：`docs/pilot/evidence/p2-stage-acceptance-2026-07-29.md`。

P2 阶段状态：已完成。下一阶段：P3 人工运营闭环验收。

### P2 残余事项

- `dispatcher02` 首次正式使用时必须完成个人密码修改。
- 电话/微信联系信息和每日轮班安排由调度主管人工维护；系统当前未单独持久化微信账号和逐日轮班表。
- P2 完成不等同于生产试点批准；仍需完成 P3、P4 和 P5。

## P3 试点运行准备

用户确认使用模拟验收数据，并要求旧 3 笔人工复核订单统一拒绝。

### P3 启动前清场

- 旧订单 `77d6d309…`、`cfec2569…`、`7761fa69…` 已通过页面统一拒绝。
- 三笔订单最终均为 `UNSERVICEABLE`，拒绝原因统一为“P3 验收前清理历史测试样本”。
- 清场后待人工复核订单为 0、车辆任务为 0，三条 `MANUAL_REVIEW_REJECTED` 审计完整。

### P3-1 即时需求录入

- 模拟订单 `dc6a080d-e8a9-4e66-ba4c-9dacf0e0b2c9` 已通过页面创建。
- 起点高铁站、终点县政府均通过服务区校验并匹配已启用虚拟站点。
- 人数、即时类型、模拟联系方式、坐标、站点外键和预计出发时间已只读核对。

P3-1 状态：已完成。

### P3-2 调度与人工复核

- 订单 `dc6a080d…` 产生 4 个候选方案，最佳分数 85.59，高于自动阈值 82。
- 地图路径仍降级为 `STRAIGHT_LINE`，系统因 `MAP_ROUTE_UNAVAILABLE` 强制进入人工复核。
- `dispatcher02` 已确认人工方案，生成任务 `0ecc7ce6-c6cb-451e-8583-e046d83bdef7`。
- 人工复核路径及审计已完成；自动派单路径未完成。

P3-2 首次验收状态：部分完成，受地图路由降级阻塞；后续已使用获批的本地模拟路由完成自动派单复验。

### P3-3 模拟下达与反馈台账

- 本次没有真实电话/微信通联，按模拟验收口径记录。
- 任务下达时间 07:36:53、模拟驾驶员反馈时间 07:37:00、调度员系统录入时间 07:39:41。
- 位置历史页面分别显示驾驶员反馈时间和系统录入时间，人工录入延迟未混入算法预计等候时间。

P3-3 状态：已完成（模拟通联口径）。

### P3-4 任务节点与位置事件链

- 任务 `0ecc7ce6…` 已完成发车、到达上车点、上车、到达下车点、下车、完成共 6 个位置事件。
- 两个节点最终为 `BOARDED`、`ALIGHTED`；任务和订单均为 `COMPLETED`。
- 6 个位置事件、最新位置快照和 6 条任务审计一一对应。
- 发现任务完成弹窗默认携带站点 ID，但后端任务级完成动作不接受站点 ID；验收时切回手工坐标后完成。

P3-4 状态：已完成，保留完成弹窗默认值冲突缺陷。

### P3-5 异常演练

- 取消单 `3779b93b…`：订单 `CANCELLED`，审计 `ORDER_CANCELLED`。
- 爽约单 `9f9bec16…`：订单 `EXCEPTION_CLOSED`，审计 `ORDER_NO_SHOW`。
- 故障单 `6230ee65…`：订单 `EXCEPTION_CLOSED`、任务 `178b2307…` 为 `EXCEPTION`，审计 `TASK_EXCEPTION`。
- 延误单 `e82d5e69…`：订单 `EXCEPTION_CLOSED`、任务 `a713124d…` 为 `EXCEPTION`，审计 `TASK_SEVERE_DELAY`。

P3-5 状态：动作已完成；爽约订单关闭后任务仍待发车，存在联动缺陷。

### P3-6 一致性复核

- 订单页、任务页、位置历史和调度工作台已完成页面核对。
- 主链订单、任务、节点、位置历史和审计状态一致。
- 爽约订单 `9f9bec16…` 已异常关闭，但任务 `f41f370b…` 仍为 `DISPATCHED`。
- 主链、爽约、故障、延误 4 个任务全部分配给甘J16396D，车辆仍显示 `IDLE`、可调度，未阻止冲突任务重复分配。
- `dispatcher02` 不具备 `AUDIT_READ`、`METRICS_READ`，审计页和运营看板未完成该账号下的页面级复核。
- `dispatcher02` 仍标记 `must_change_password=true`，但登录后未强制进入改密流程。
- 已完成主链订单 `dc6a080d…` 仍保留 `failure_reason=MAP_ROUTE_UNAVAILABLE`，失败原因字段未随人工确认和任务完成清除。

P3-6 首次验收状态：未通过；下述方案 A 修复复验已消除全部阻塞项。

### P3 当前结论

P3 首次验收结论：部分完成、不可收口。后续修复和最终收口证据见下述“P3-6 方案 A 修复复验”及 `docs/pilot/evidence/p3-pilot-readiness-execution-2026-07-30.md`。

首次验收确认的收口条件包括自动派单样本、爽约任务联动、车辆任务占用一致性、完成弹窗修复、主管/审计账号页面复核和首次登录强制改密复核；现已全部满足。

### P3-6 方案 A 修复复验

- 已按测试先行方式完成本地模拟路由、资源占用/释放、爽约任务联动、完成弹窗、历史失败原因清理和首次登录强制改密修复。
- 已修复真实算法回传新任务候选合成 `taskId` 时误报“算法返回的任务不存在”的 409，并新增真实协议回归测试；派单域 8 项测试通过。
- 自动派单模拟订单 `b41b9827-c0ef-4278-b7b4-2cc0bfde2a31` 产生 `AUTO_DISPATCH` 决策 `e1e18df2-2a15-40a6-a856-345de0bfe1f5` 和任务 `24b22cce-d1b2-432c-a8c8-3d1333bbea82`；地图记录为 `AMAP` 且未降级。
- 上述任务已完成发车、到站、上下客和完成 6 个事件；订单/任务均为 `COMPLETED`，失败原因为空，4 车和 4 名驾驶员全部释放。
- 爽约复验订单 `2aa9bd81-1304-46f8-8bd0-7fc09ffb2fe4` 自动派单后执行爽约；订单为 `EXCEPTION_CLOSED`，任务 `775d2d43-c314-4fec-96d6-c83c2dd8cfa9` 及两站均为 `CANCELLED`，原因和三条相关审计完整，资源全部释放。
- `dispatcher02` 保持最小权限，仅有 `DISPATCHER`；真实前端登录后已强制进入首次改密页。
- 用户已亲自完成 `SYSTEM_ADMIN` 首次改密；`admin` 已启用、`must_change_password=false`，角色为 `SYSTEM_ADMIN`。
- `SYSTEM_ADMIN` 已完成运营看板和审计日志页面验收：看板指标与数据库同口径复算一致；审计页筛选 `ORDER_AUTO_DISPATCHED` 精确返回两条 P3-6 自动派单记录，并可查看完整任务链和爽约取消审计。
- 最终回归：后端 208 项测试失败/错误 0（跳过 23）；前端 173 项测试、类型检查和生产构建通过；模拟路由 5 项测试通过；Compose 配置、API 健康、差异检查和资源终态均通过。

P3 当前状态：已完成并收口。方案 A 最小权限设计保持不变，`dispatcher02` 未增加审计或看板权限。
## P4 三项 P0 阻断项与首日启动门禁（2026-07-30）

- 位置事件容量验证通过：隔离 PostGIS 中通过应用服务写入 10,000 条事件；写入 210,830 ms、历史查询 1,269 ms、CSV 导出 960 ms、快照查询 34 ms。
- 数据库备份恢复演练通过：成功备份并在 `template0` 空库恢复；12 项迁移、4 车、4 驾驶员、31 站点一致，三类孤儿为 0，源数据库与 API 未中断。
- 告警通知链路通过：`dispatcher02` 正式接口区外上报告警成功，随后恢复区内快照；管理端位置历史和审计页显示两条新增记录，审计增量为 2。
- 首日资源门禁通过：API/前端/算法/模拟路由/数据库运行正常，31/31 站点在已发布服务区内，4 车 `IDLE`、4 驾驶员 `AVAILABLE`、活动任务 0、重复活动任务 0，规则参数未变化。
- 最终回归通过：后端 209 项（失败 0、错误 0、跳过 24），前端 31 个测试文件 173 项，类型检查、生产构建和模拟路由 5 项均通过。
- `dispatcher02` 首次改密已完成；最终只读复核确认账号启用、`mustChangePassword=false`，角色仍仅为 `DISPATCHER`。
- 首日资源终态复核：4/4 车辆 `IDLE` 且可调度，4/4 驾驶员 `AVAILABLE` 且资质为 `QUALIFIED`，活动任务 0、同车重复活动任务 0；API `UP`、前端 HTTP 200、模拟路由 `UP`。
- 详细证据：
  - `docs/pilot/evidence/p4-capacity-validation-2026-07-30.md`
  - `docs/pilot/evidence/p4-backup-restore-drill-2026-07-30.md`
  - `docs/pilot/evidence/p4-alert-chain-validation-2026-07-30.md`
  - `docs/pilot/evidence/p5-day-1-readiness-2026-07-30.md`

P4 技术验证状态：已完成。首日真实订单启动门禁：已解除，允许按 5–10 笔/日进入试运行。

## P5 首日试运行（2026-07-30）

- 首笔真实订单已录入，订单标识前缀 `7171c50d`；自动派单后于 12:37:45 按“乘客未到”关闭，订单终态为 `EXCEPTION_CLOSED`。
- 自动派单成功：4 个候选，评分 85.59，预计等待 1 分钟、绕行 3 分钟，地图路由未降级。
- 已生成车辆任务 `a7db6bba`，最终为 `CANCELLED`；2 个任务站点均取消，审计包含 `TASK_CANCELLED_NO_SHOW` 和 `ORDER_NO_SHOW`。
- 本单没有位置事件或实际到站时间，不能记录为已发车、已到达或已完成；实际等待和 ETA 偏差不可计算。
- 当前资源：4/4 车辆 `IDLE`、4/4 驾驶员 `AVAILABLE`、活动任务 0，资源释放正常。
- 当前异常：1/1，为乘客爽约；没有地图降级、系统错误或重复资源占用。
- 活动任务统计口径统一为 `PENDING_DEPARTURE`、`DISPATCHED`、`IN_PROGRESS`、`PAUSED`，不再使用遗漏 `DISPATCHED` 的旧查询。
- 联系方式字段系统仅做非空校验，后续每笔真实订单继续执行提交前人工核对。
- 运营台账：`docs/pilot/evidence/p5-day-1-operation-log-2026-07-30.md`。
- 第二笔真实订单 `05490907…` 已完成提交和自动派单：4 个候选，评分 85.59，预计等待 1 分钟、绕行 3 分钟，地图路由 `AMAP` 且未降级；生成任务 `aa97ff80…`。
- 第二单于 12:48:42 被 `dispatcher02` 按“乘客未到”关闭，早于计划出发 12:54 和计划上车 12:55；订单为 `EXCEPTION_CLOSED`，任务和 2 个站点均为 `CANCELLED`。
- 2026-07-30 当班人员确认第二单的“乘客未到”为人工误操作，异常归因已闭环；系统仍需补充爽约动作的时间/状态门禁和二次确认。
- 当前首日累计：真实订单 2、自动派单 2、人工复核 0、完成 0、爽约异常 2、地图降级 0；4/4 车辆 `IDLE`、4/4 驾驶员 `AVAILABLE`、活动任务 0。

P5 当前状态：第二单误操作归因已闭环，当前资源已释放；在系统防误操作整改上线前，后续订单按人工操作规程控制爽约动作。

### P5 “乘客未到”防误操作整改（代码完成，待运行环境验收）

- 已按测试先行完成 5 分钟默认等候期、订单/任务/到站/上车状态强制门禁；等待起点取计划上车时间与实际到站时间的较晚值。
- 后端提交时重新校验资格，拒绝请求返回 `409` 并写入 `ORDER_NO_SHOW_REJECTED`；合法操作继续联动关闭订单、取消本单站点/任务、释放资源并写成功审计。
- 请求增加幂等键；同一订单的并发爽约操作通过数据库订单行锁串行化，自动化用例确认只能一次成功。
- 订单列表返回服务端资格快照、最早操作时间、已等待秒数和禁用原因；前端按服务端资格显示按钮、剩余时间和二次确认对话框，未选择固定原因不能提交。
- 实现提交：`257b572`、`34f1b0f`、`ca3217a`、`feceb54`、`d0b3ab8`、`37fa95b`。
- 自动化回归与构建已通过：后端 221 项（失败 0、错误 0、跳过 24），前端 32 个测试文件共 176 项，类型检查和生产构建通过；真实浏览器和当前本地试点运行环境尚未部署/验收，本次未创建或修改任何真实订单。

P5 防误操作整改状态：代码与自动化验证完成；运行环境上线和真实浏览器验收待单独授权执行。

## 试运行调度优化部署与真实验收（2026-08-09）

### 代码与版本状态

- 当前分支：`codex/p1-vehicle-location-calibration`。
- 本轮代码 HEAD（不含本进度记录提交）：`e7a325b174d434bedf8467608ba12a911c1bee1a`（`fix: defer task stop sequence validation`）。
- 本轮已完成并提交的主要变更：
  - `3aa5594`：地图点选且地址为空时，不再写入“地图点选位置”占位文本。
  - `2b98451`：车辆任务页以车牌号码显示车辆。
  - `f262708`：按真实路线重算既有任务插单，优先复用可行任务并计入车辆启用成本。
  - `4261ff4`：补充试运行验收记录。
  - `e7a325b`：将任务站点序号唯一约束改为事务提交时延迟校验，修复插单持久化冲突。
- 除本次 `progress.md` 进度记录外无未提交改动；本文件将单独纳入 Git 提交，提交后工作树应为干净状态。

### 自动化验证

- 算法测试：11 项通过。
- API 全量 Maven 测试退出码为 0；新增迁移测试按测试先行方式先因缺少 V12 明确失败，补充迁移后通过。
- 前端顺序执行 32 个测试文件、182 项测试全部通过；类型检查和生产构建通过。
- 前端构建仍有单个 JavaScript chunk 超过 500 kB 的既有提示，不阻断本地试运行。
- 隔离 PostGIS Testcontainers 用例因当前 Windows Docker 探测限制被跳过，未计为通过；实际试点 PostGIS 数据库已通过约束状态查询和真实插单验收补足验证。

### 本地部署状态

- 前端 `http://127.0.0.1:5174`、API `http://127.0.0.1:18081`、算法 `http://127.0.0.1:18090`、本地模拟路由 `http://127.0.0.1:18091` 均恢复运行；API 健康状态为 `UP`。
- 算法已部署镜像 `drt-ops-pilot-algorithm-local:20260809`；旧算法容器保留为已停止的 `drt-ops-pilot-algorithm-backup-20260809`，便于必要时回退。
- 本地 API 调试容器显式设置 `SPRING_FLYWAY_ENABLED=false`，因此已对实际试点库 `drt_ops_pilot_bootstrap` 以单事务手工应用 V12；约束 `task_stops_vehicle_task_id_sequence_number_key` 当前为 `DEFERRABLE=true`、`INITIALLY DEFERRED=true`。
- 因 Flyway 在该调试容器中关闭，V12 尚未写入本地库的 `flyway_schema_history`；以后使用标准 Flyway 启动时仍会正常执行同一迁移。若重建当前调试库且继续关闭 Flyway，需要重新执行迁移流程。

### 真实浏览器验收结果

- 使用现有 `dispatcher01` 登录会话完成验收。
- 地址修复通过：起点地图点选后坐标和推荐站点正常生成，地址框保持为空，没有再自动写入“地图点选位置”；补充实际地址后可正常提交。
- 车牌展示通过：车辆任务 `b58b0427-af60-495e-9b0c-2e2e4df49ae6` 在车辆任务页显示 `甘J00856D`，不再显示车辆 UUID。
- 创建三笔明确标注的本地验收单：
  - A：`27aeeecb-6599-4a3c-8307-6f83d3b0237d`，1 人。
  - B：`8f16c2fd-a2a2-472b-9d76-2fb543e83218`，2 人。
  - C：`d519dde4-aafb-41f7-b643-bc0fb965b6fc`，1 人。
- A 自动派单评分 86.11，创建新任务；B 自动派单评分 85.81，`candidateType=EXISTING_TASK`、`activationCost=0`、`selectionReason=EXISTING_TASK_PREFERRED`。
- B 首次插单暴露任务站点序号唯一键冲突，事务完整回滚、订单保持待调度；补充 V12 并重新部署后，同一订单重试成功。
- C 评分 81.70，按阈值进入人工复核；候选仍为同一任务、同一车辆，人工确认后成功插入。
- 三笔订单最终共同使用任务 `b58b0427…` 和车辆 `甘J00856D`；共 6 个任务节点，序号 1–6 唯一，验证了优先插入、合乘容量、路线重算和零车辆启用成本路径。
- 人工复核审计完整：`ORDER_PENDING_MANUAL_REVIEW`、`MANUAL_REVIEW_APPROVED` 均已记录。

### 验收数据清理与审计说明

- 三笔验收单已取消，验收任务已变为 `CANCELLED`，`甘J00856D` 已释放为 `IDLE`；未操作历史订单。
- 订单取消页的“确认乘客取消”会固定写入“乘客取消”。误点 A 后保留原始不可变审计，并追加 `ORDER_CANCELLATION_REASON_CORRECTED`，明确原因为“本地验收数据清理（非真实乘客取消）”；B、C 未执行乘客取消原因确认。

### 下一步计划

- 继续按 5–10 笔/日推进试运行，重点监控既有任务插入成功率、自动/人工复核比例、峰值载客量、绕行分钟数和车辆启用数量。
- 首日继续观察 V12 后的多单插入；若再次出现不可服务或持久化错误，按既定规则记录并立即查明原因。
- 后续优化取消原因界面，使运营人员可选择“乘客取消”“验收数据清理”等真实原因，避免硬编码审计语义。
- 稳定观察后再决定是否删除已停止的旧算法备份容器；当前不执行破坏性清理。

## 试运行批次记录（2026-08-09）

- 本批按 5–10 笔/日节奏录入 3 笔真实试运行订单，登录账号为 `dispatcher02`：罗老师 `4aaf70da`、随缘 `40310924`、刘盛美 `86b745fc`。
- 三笔订单均已完成调度并完成全流程，状态均为 `COMPLETED`；站点匹配分别为：高铁站→陇阳镇、姜滩小学→安川、中医院→陇阳镇。
- 罗老师订单复核：关联车辆甘J18817D，任务状态 `COMPLETED`，高铁站上车节点 `BOARDED`、陇阳镇下车节点 `ALIGHTED`，无不可服务或异常关闭记录。
- 本批未修改历史订单、车辆资源或调度规则；订单中心已保留为后续批次录入入口。

### 下一步计划

- 继续按每日 5–10 笔推进；下一批录入前逐笔核对电话、实际起终点、坐标和乘客认可的虚拟站点。
- 录入后再按顺序执行调度，并记录订单状态、任务/车辆关联、人工复核和异常原因。

## 订单与车辆任务按创建日期分组（2026-08-09）

- 当前分支：`codex/p1-vehicle-location-calibration`。
- 实现提交：`e977839`（`feat: group orders and tasks by creation date`）。
- 变更内容：后端订单/车辆任务视图补充 `createdAt`；订单中心按本地日期分为“今日新增订单”和“历史订单”；车辆任务按本地日期分为“今日新增任务”和“历史任务”。旧数据缺少创建时间时归入历史分组。
- 测试先行：先补充 API 与前端失败断言，再实现字段和界面分组；前端全量 32 个测试文件、184 项通过；后端全量退出码 0，报告为 248 项执行、24 项跳过、失败/错误 0；类型检查、生产构建通过。
- 本地验收：重启 `drt-ops-pilot-web` 前端容器后，订单中心和车辆任务页均显示分组标题；今日无新增时显示 0 条，历史记录独立显示，车辆列显示车牌号码。API/数据库未重启或写入业务数据。
- 未提交改动：进度记录待提交；提交后应为干净工作树。

### 下一步计划

- 保持按 5–10 笔/日推进试运行；录入前直接进入“今日新增”分组核对新单，历史订单/任务仅作查询。
- 继续监控创建时间缺失数据及跨午夜边界；若发现旧接口未返回 `createdAt`，优先补齐接口兼容处理。

## P5 阶段总结与收口（2026-08-09）

### 阶段目标与统计口径

- P5 目标：在本地隔离试点环境按 5–10 笔/日逐步运行，验证订单录入、人工核对、调度/合乘、任务节点、位置事件、异常处置和审计闭环。
- 本节仅统计真实试运行订单；历史模拟验收订单不纳入 P5 业务指标。首日异常样本与整改后稳定样本分开记录。

### 首日基线（整改前）

- 2026-07-30 录入 2 笔真实订单，均自动派单成功，但均按“乘客未到”关闭；订单终态为 `EXCEPTION_CLOSED`，任务及站点取消，完成数为 0。
- 其中第二笔已由当班人员确认属于计划出发前的人工误操作；资源随后正常释放，未发现地图降级、系统错误或重复车辆占用。

### 整改与稳定运行结果

- 已部署并验收 5 分钟默认等候门禁、订单/任务/到站/上车状态校验、二次确认、固定原因、服务端复核、幂等键、并发互斥和审计记录。
- 整改后 2026-08-09 连续两批共 6 笔真实订单全部完成，当前状态均为 `COMPLETED`；未出现不可服务或异常关闭。
- 两批订单均验证了既有任务插入和合乘：第一批 3 笔使用 2 个任务，第二批 3 笔共用一个既有任务和同一车辆，均形成完整 6 节点链。
- 两笔订单进入人工复核，原因均为评分低于自动派单阈值 82 但仍在人工复核区间；人工确认后均成功插入既有任务。

### P5 KPI 快照

| 指标 | 累计结果 | 说明 |
| --- | ---: | --- |
| 真实试运行订单 | 8 笔 | 2 笔首日异常 + 6 笔整改后稳定样本 |
| 订单完成 | 6 笔（整改后 6/6） | 稳定运行样本完成率 100% |
| 异常关闭 | 2 笔 | 均为首日“乘客未到”，其中 1 笔确认是误操作 |
| 自动派单 | 6 笔 | 含首日 2 笔和整改后 4 笔 |
| 人工复核 | 2 笔 | 2 笔均确认后完成 |
| 不可服务 | 0 笔 | 整改后样本为 0 |
| 重复资源占用 | 0 笔 | 未发现同车重复活动任务 |
| 目标节奏 | 达标 | 2026-08-09 当日 6 笔，处于 5–10 笔/日区间 |

### 运行边界与遗留风险

- 当前仍是本地隔离试点，不等同于生产上线批准；需继续保持最小权限和人工运营闭环。
- 每笔订单提交前继续人工核对电话、实际起终点、坐标和乘客认可的虚拟站点；曾出现姓名资料版本不一致，需作为数据质量门禁保留。
- ETA 继续按“仅作参考”使用；尚未将人工位置阶段的 ETA 偏差作为硬性运营指标。
- 后续重点观察既有任务插入成功率、人工复核率、峰值载客量、绕行分钟数、车辆启用数量和不可服务原因。

### P5 收口结论

P5 试运行已达到“整改后稳定运行”阶段，用户确认结束 P5；本阶段记录、异常归因、整改验证和真实订单结果均已保留。后续转入 P6 计划，不再把首日误操作样本与整改后稳定样本混合解读。

## P6-1 真实路径服务与 ETA 能力专项评估（2026-08-09）

### 连通性与样本结果

- 使用标准 API 容器中注入的真实 Key 完成连通性预检：两次驾车路径调用及一次地理编码调用均返回成功状态；预检路线为 389 米、122 秒。
- 标准健康栈中的 6 个北京演示站点未被用作样本；样本事实源为通渭试点数据库中 31 个启用、已核验且位于已发布服务区内的虚拟站点。
- 48/48 组候选路线预分类成功；按八扇区轮询规则重新固定短距离 5 组（264–1377 米）、中距离 10 组（1458–2336 米）、长距离 5 组（2995–17285 米），各档分别覆盖 5/6/5 个方向扇区且距离边界严格有序。
- 20 组固定路线各重复调用 10 次，共 200 次；成功 200、失败 0、降级 0，最小/中位/P90/最大延迟分别为 1131/1361/2330/5621 毫秒。
- 距离偏差告警为 0；ETA 告警为 S03 第 10 次相对首次正式样本变化 31.71%，对应 41 秒到 28 秒，绝对差 13 秒。
- 20/20 路线形状摘要有效，路径点数为 9–482，20 个 SHA-256 路线指纹互不重复；证据未保存完整路线坐标。

### 故障保护与数据边界

- 无效 Key、配额超限、超时、网络失败、响应缺字段、非法经纬度、直线降级、调度保护、证据不变量和异常脱敏均已纳入自动化覆盖；专项 35 项、API 全量 265 项、算法 11 项、管理端 Vitest 184 项均通过，管理端类型检查和生产构建通过。
- 管理端 Playwright 的固定登录到期时间、固定端口复用及业务夹具问题已由独立 PR #13 修复；完整 Playwright 5/5 通过，不再保留 1/5 的旧状态。
- 非法经纬度现会在 HTTP 调用前拒绝；真实路径不可用时只允许 `STRAIGHT_LINE` 且标记 `degraded=true`，调度继续进入 `MAP_ROUTE_UNAVAILABLE` 保护，不自动派单。
- 证据目录敏感信息扫描结果为 0；真实 Key 未写入输出、文件或 Git。
- 通渭样本库采样前后有效站点数均为 31；本次没有创建或修改订单、任务、车辆、驾驶员、规则、服务区或位置事件。

### 实现与证据

- 设计、原实施计划和发布整改计划位于 `docs/superpowers/specs/` 与 `docs/superpowers/plans/`。
- 主要证据位于 `docs/pilot/evidence/p6-1-*`，包括站点快照、预分类、固定样本、200 次逐调用明细、汇总、路线指纹、评估报告和匿名运营偏差台账。
- 未推送历史已在本地重写；最终提交只保留彻底脱敏后的证据和整改后的实现，不再引用已淘汰的中间提交哈希。

P6-1 当前状态：**人工审阅已完成，P6-1 已正式收口**。上车点绝对误差中位数/P90 为 300/660 秒，行程绝对误差中位数/P90 为 557/1214 秒，行程 MAPE 为 207.6181%，两类时长方向均为高估 5 笔、低估 5 笔。ETA 继续仅作参考，不升级为硬性运营指标；P6-2 未启动，须另行制定规格和实施计划。

## 调度工作台地图界面修复（2026-08-11）

- 在隔离分支 `codex/dispatch-map-workbench` 完成 A「地图主导 + 车辆侧栏」与 C「全景指挥浮层」的混合界面；未修改订单、任务、位置事件或其他业务数据。
- 地图容器和 Leaflet 画布改为 100% 等高，并通过 `ResizeObserver` 触发 `invalidateSize`；修复列表内容拉高网格后地图下方出现空白的问题。
- 服务区、虚拟站点、任务节点链和车辆统一由 Leaflet 经纬度图层渲染；移除按百分比绝对定位的伪车辆卡片。数据刷新和图层切换不再自动重置调度员已经平移/缩放的视野，保留“回到全局”显式操作。
- 车辆标记按状态使用绿色空闲、蓝色执行中、红色离线/越界；点击标记显示车牌、中文状态、8 位短任务号和最后位置时间，不展示完整任务 UUID。右侧车辆侧栏与地图标记双向联动，无有效坐标的车辆禁用定位。
- 工作台调整为左侧精简订单/复核/任务、中间全高地图、右侧车辆列表；地图浮层显示待调度、待复核和执行中车辆数量。三个图层开关均已避开 Leaflet 原生缩放控件并可独立操作。
- 回归结果：管理端类型检查通过；Vitest 33 个测试文件、192 项全部通过；`dispatch-flow.spec.ts` 3/3 通过。浏览器在 1366×768、1920×1080 下确认地图与画布等高且底部无空白，768×1024 下无横向溢出。
- 业务边界不变：车辆位置仍来自后端最新人工上报快照，不代表 GPS 连续轨迹；人工节点链继续明确标注为离散上报节点。P6-2 仍未启动。

## 管理端登录韧性修复（2026-08-11）

- admin 账号的临时密码重置与首次改密已由用户本人完成；项目文件、提交记录和排障文档均未保存临时密码、新密码、令牌或 Cookie。
- 已确认此前的两个独立根因：`5176` 对应 Origin 未列入 API 允许来源；Chrome 密码管理器在 `5174` 页面提交了已保存的旧密码，导致真实 API 返回 401。
- 新版前端在 `5174` 连接真实 API 后已完成登录验证。前端现将认证失败、来源拒绝、网络故障、未知异常和认证后导航失败分别标记为 `LOGIN-401`、`LOGIN-ORIGIN-403`、`LOGIN-NETWORK`、`LOGIN-UNKNOWN` 和 `LOGIN-NAVIGATION`；401 会清空并重新聚焦密码框，首次改密用户会直接进入修改密码页。
- API 的本地允许来源改为由 `DRT_WEB_ALLOWED_ORIGINS` 显式配置，默认精确覆盖 `localhost` 与 `127.0.0.1` 的 5173、5174、5176 端口，继续拒绝未知来源和通配符配置；Docker pilot 配置同步声明该变量。
- 测试先行证据：登录页专项 6/6 通过，登录/路由/改密联合回归 11/11 通过，CORS 专项 8/8 通过；详细排查流程见 `docs/pilot/admin-login-troubleshooting.md`。
- 实现提交依次为 `4921789`（登录失败分类）、`0fb2d73`（认证与导航失败分离）、`6612a2c`（本地来源显式配置）。本次修复不改变 P6-1 的评估结果与 ETA“仅作参考”结论，P6-2 仍未启动。

## 运营看板界面优化（2026-08-13）

### 隔离实施与范围

- 独立工作树：`D:\codex-projects\.worktrees\admin-ui-optimization`；分支：`codex/admin-ui-optimization`，未改动 P6-2 工作树。
- 功能分支已干净重放到本地 P6-2 最新提交 `7d1fd2c`，重放过程无冲突；运营看板实现提交为 `b6e7550`。
- 本阶段仅实施已确认的运营看板优化，不把基础数据导航、订单中心、车辆任务、位置历史、资源/规则配置、审计日志、用户权限和全局页面缓存等后续页面工作混入本次交付。

### 已实现内容与统计口径

- 首屏采用四个核心指标卡：订单量、完成率、平均等待、车辆利用率；每张卡同时展示绝对值、比例或对比基线，并标注“正常/偏高/偏低/暂无当日数据/缺少对比基线”。顶部指标与“今日调度表现”已合并去重。
- 中部提供近 7 个上海运营日趋势图，可切换订单量、完成率、平均等待、车辆利用率；标题、横坐标日期范围和纵轴单位均由同一份数据派生，缺失值不按 0 绘制。
- 下部提供订单状态、任务状态、车辆状态三组分类占比图；百分比采用最大余数分配，显示值非负且合计严格为 100.0%。
- 后端新增 `GET /api/metrics/operations-dashboard?endDate=YYYY-MM-DD&days=7` 聚合接口。趋势窗口为 `[endDate-6, endDate]`，基线窗口为 `[endDate-7, endDate-1]`；首版仅支持 `days=7`。
- 完成率使用区间内已完成订单数/订单总数，平均等待使用全部有效等待样本，车辆利用率使用当前可调度车辆为分母；分母缺失时返回空值，不伪造 0%。状态阈值按 12 位内部精度判断，接口边界再四舍五入。
- 页面具备骨架屏、重试、刷新时保留旧快照、全局 Toast 成功/失败反馈及响应式布局；车辆指标明确标注“按当前可调度车辆口径”。

### 验证与审阅证据

- 后端全量测试退出码 0：72 个测试套件、348 条用例，失败 0、错误 0、跳过 27；其中运营指标服务专项 7/7 通过。
- 前端按单 worker 分四组复测，合计 42 个测试文件、230 条用例全部通过：API/展示/状态/认证/路由 42 条，组件 97 条，页面 81 条，地图/布局 10 条。
- 管理端类型检查与生产构建通过；构建产物主 JS 为 683.97 kB，保留 Vite 超过 500 kB 的分块告警，后续应做按路由拆包。
- 浏览器使用虚构数据完成视觉验收：桌面首屏可见四张指标卡和趋势图；1024 与 760 像素宽度无横向溢出；趋势模式标题/单位切换、分类百分比及 Toast 均正常，控制台无错误。760 像素下既有导航占用较多，建议看板主要在不低于 1024 像素宽度使用。
- 独立代码审阅未发现需要阻止本次试点交付的缺陷。已修复无当日样本误报正常、阈值使用舍入值、车辆分母语义不清和占比尾项可能出现 `-0.1%` 等问题；全量读取的性能风险作为已知边界保留。

### 已知边界与发布状态

- 首版聚合按已批准方案使用仓储全量读取后再按日期归集，只适用于当前试点数据规模；数据量增长前必须改为数据库分组聚合、索引和查询上限。
- `npm audit` 仍报告 19 个既有依赖漏洞（9 个 moderate、10 个 high）；本次未改依赖，未把依赖升级风险混入看板交付。
- 本地 `gh` 的 GitHub 令牌仍失效；Git Credential Manager 可正常推送，GitHub 应用可用于只读核验。创建 PR 时应用写入权限返回 403，最终通过已登录的 Chrome 会话创建，未读取或复制令牌。
- P6-2 已提交分支 `codex/p6-2-jt-active-safety-spec` 已推送到远端，base SHA 为 `7d1fd2c`。P6-2 工作树中的 3 个未提交文件保持原状，未暂存、未提交、未推送。
- 运营看板草稿 PR 已创建：[PR #18](https://github.com/idavy2028-coder/drt-ops-mvp/pull/18)。只读核验结果为 `open`、`draft=true`、`mergeable=true`；base 为 `codex/p6-2-jt-active-safety-spec`，head 为 `codex/admin-ui-optimization`，创建时包含 9 条提交和 23 个变更文件。

### 下一步计划

- 审阅 PR #18，重点复核指标统计口径、首版全量读取性能边界、当前可调度车辆分母和依赖安全告警；P6-2 未提交改动继续由原任务独立处理。
- 如需继续使用 GitHub CLI，再执行 `gh auth login -h github.com` 恢复 `gh` 凭据；不影响当前 PR 的网页审阅。
- 本看板 PR 独立审阅后，再按原界面优化清单逐页实施其余管理端优化，不扩大当前提交范围。
