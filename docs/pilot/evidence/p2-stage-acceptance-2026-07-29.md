# P2 运营基础数据校准阶段验收证据

**验收日期：** 2026-07-29（Asia/Shanghai）
**代码基线：** `codex/p1-vehicle-location-calibration`，`439228bb45e1f9a9c922f794d626e7f5561d30e0`
**数据环境：** 本机管理端 `127.0.0.1:5174`、API `127.0.0.1:18081`、隔离 PostGIS 数据库 `drt_ops_pilot_bootstrap`
**业务确认人：** 用户代表调度主管、系统管理员完成本次确认

## 验收范围

本记录汇总 P2-1 至 P2-6：

1. 服务区边界复核。
2. 虚拟站点补齐与导入核验。
3. 站点启用清单和安全说明确认。
4. 车辆与驾驶员台账确认。
5. 第二调度员账号验收。
6. 调度规则参数确认。

## P2-1 服务区边界

- P2-1 初次业务确认针对发布版本 v5，确认当时边界覆盖实际运营范围。
- P2-2 新增的 5 个 v5 区外站点后续由用户改为启用，运营站点范围因此发生变化。
- 管理员于 2026-07-29 19:21:41 发布 v6，审计动作是 `SERVICE_AREA_PUBLISHED`。
- 最终只读 PostGIS 核验结果：
  - 已发布版本和草稿版本均为 6，二者几何相同。
  - v6 几何有效，服务区已启用。
  - 31/31 个已启用站点均位于 v6 内，区外启用站点为 0。
- 真实浏览器资源配置页显示“已发布 v6”和“站点覆盖：31 个，服务区内 31 个，服务区外 0 个”。

## P2-2 虚拟站点补齐

- 站点总数为 31，符合 30 至 50 个的计划要求。
- 名称、地址、GCJ-02 坐标、服务半径、上下车能力、核验时间和启停状态已校验。
- 本批 CSV 首次导入创建 10 条；同批重复提交跳过 10 条，未生成重复站点。
- 详细导入证据见 `docs/pilot/evidence/p2-2-virtual-stop-import-review-2026-07-29.md`。

## P2-3 启用清单与安全说明

### 最终清单

| 分类 | 数量 | 结论 |
| --- | ---: | --- |
| 启用 | 31 | 纳入试点站点清单 |
| 暂不启用 | 0 | 无 |
| 待复核 | 0 | 无 |

订单匹配实现 `VirtualStopMatcher` 对自动匹配和指定站点匹配均检查 `VirtualStop::isEnabled`；未启用站点不会进入订单匹配。

### 安全说明

用户确认 31 个站点统一采用：

> 经运营复核，仅在指定安全停靠区域上下客，并服从现场交通管理。

31 个站点于 2026-07-29 19:36:42 至 19:40:11 通过资源配置页面逐项更新：

- 31/31 个站点保存后保持启用。
- 31/31 个站点安全说明与确认文本一致。
- 旧“示例数据，需现场核验”占位说明为 0。
- 页面逐项更新产生 `VIRTUAL_STOP_UPDATED` 审计记录。

## P2-4 车辆与驾驶员台账

用户于 2026-07-29 明确确认：

- 4 辆车、4 名驾驶员信息有效。
- 联系方式已经人工核对。
- 班次按每日 06:30–19:00 轮班制执行。

数据库交叉核验：

- 4/4 辆车为空闲且可调度。
- 4/4 名驾驶员状态为 `AVAILABLE`，资质状态为 `QUALIFIED`。
- 4/4 名驾驶员均已登记联系电话。

电话/微信核对和轮班安排属于本次人工业务确认；验收证据不记录个人联系方式明文。

## P2-5 第二调度员账号

账号验收结果：

| 项目 | 结果 |
| --- | --- |
| 用户名 | `dispatcher02` |
| 显示名称 | `调度员：dispatcher02` |
| 状态 | 启用 |
| 角色 | 仅 `DISPATCHER` |
| 首次改密 | 必须 |
| 登录 | 成功 |
| 订单读取 | HTTP 200 |
| 任务读取 | HTTP 200 |
| 车辆位置读取 | HTTP 200 |
| 用户管理 | 被拒绝 |
| 规则管理 | 被拒绝 |

`DISPATCHER` 角色代码权限包含订单创建/读取、调度执行、人工复核、任务读取/执行、决策读取和位置读取/上报；不包含 `USER_MANAGE`、`RULE_MANAGE` 或 `RESOURCE_MANAGE`。

管理员修正显示名称产生 `USER_PROFILE_UPDATED` 审计；两次越权验证产生 `AUTHORIZATION_DENIED` 审计。验收文档未记录临时密码或访问令牌。

## P2-6 调度规则

用户确认并由页面、数据库交叉核对：

| 参数 | 最终值 |
| --- | ---: |
| 最大等候 | 5 分钟 |
| 最大绕行 | 8 分钟 |
| 预约窗口 | 60 分钟 |
| 自动派发阈值 | 82 |
| 人工确认阈值 | 62 |
| 等待权重 | 0.35 |
| 绕行权重 | 0.20 |
| 稳定性权重 | 0.30 |
| 利用率权重 | 0.15 |
| 插单策略 | `REALTIME_INSERTION` |
| 规则状态 | 启用 |

## 自动化回归

- 前端：`resources-page.test.ts`、`user-management-page.test.ts`、`rules-page.test.ts` 共 3 个文件、12 项测试通过。
- 后端：`VirtualStopMatcherTest` 3 项、`VirtualStopImportServiceTest` 2 项、`UserManagementApiTest` 8 项、`DispatchRuleSetApiTest` 6 项，共 19 项测试通过，失败 0、错误 0、跳过 0。
- 后端测试首次因当前工作树不存在预期 Maven 相对路径而未启动；定位授权工作区内的 Maven 后以相同测试选择器重跑，正常退出。

## 残余事项

- `dispatcher02` 使用临时密码创建，首次正式使用时必须完成改密；验收不代替账号持有人设置个人密码。
- 轮班安排和电话/微信联系信息由调度主管人工维护；系统当前未单独持久化微信账号和逐日轮班表。
- P2 完成不代表生产试点批准。P3 仍需按电话/微信协作方式跑通订单、调度、任务节点、异常处理和审计闭环。

## 阶段结论

P2-1 至 P2-6 的完成标准均已满足：

- v6 已发布，31 个启用站点全部位于服务区内。
- 31 个站点安全说明已由运营确认并写入。
- 4 车、4 驾驶员台账和轮班安排已确认。
- 第二调度员账号已完成登录与最小权限验收。
- 调度规则参数已由业务确认并与系统值一致。

P2 阶段状态：**已完成，可进入 P3 人工运营闭环验收。**
