# 首日试运行启动门禁记录（2026-07-30）

## 当前结论

**阻断，暂不启动首日真实订单。**

位置事件容量、数据库备份恢复、管理端与审计告警链路三项 P0 技术验证均已通过；服务、资源、规则和自动化回归均满足首日 5–10 笔订单的技术条件。唯一未关闭门禁为 `dispatcher02.mustChangePassword=true`，必须由用户完成首次改密后再做一次只读复核。

本记录不创建、不提交任何真实订单。

## 三项 P0 验证

| 验证项 | 结果 | 证据 |
|---|---|---|
| 位置事件容量 | 通过 | `p4-capacity-validation-2026-07-30.md` |
| 数据库备份恢复 | 通过 | `p4-backup-restore-drill-2026-07-30.md` |
| 管理端与审计告警链路 | 通过 | `p4-alert-chain-validation-2026-07-30.md` |

关键结果：

- 4 车共 10,000 条位置事件应用服务写入耗时 210,830 ms，历史查询 1,269 ms，CSV 导出 960 ms，快照查询 34 ms。
- 成功备份 `drt_ops_pilot_bootstrap-20260730-113756.dump`，大小 91,039 字节，SHA-256 为 `9a71090198f2cab560dd066895421d14a355ce382efd52c68ca90832c3a45fb9`。
- 恢复库与源库的 12 项迁移、4 辆车、4 名驾驶员、31 个虚拟站点计数一致，三类孤儿记录均为 0。
- 区外位置返回 `OUTSIDE_SERVICE_AREA`，随后恢复到启用区内站点；审计新增 2 条，最新快照指向区内恢复事件。

## 首日资源与服务门禁

| 门禁 | 实际状态 | 判定 |
|---|---|---|
| API | `UP` | 通过 |
| 管理前端 | HTTP 200 | 通过 |
| 本地模拟路由 | `UP` | 通过 |
| 算法、数据库与试点容器 | 运行中 | 通过 |
| 启用站点覆盖 | 31/31 位于已发布服务区内 | 通过 |
| 车辆 | 4/4 `IDLE` | 通过 |
| 驾驶员 | 4/4 `AVAILABLE` | 通过 |
| 活动任务 | 0 | 通过 |
| 同车重复活动任务 | 0 | 通过 |
| 管理员 | 启用、`SYSTEM_ADMIN`、无需改密 | 通过 |
| `dispatcher02` | 启用、仅 `DISPATCHER`、必须首次改密 | **阻断** |

## 规则基线

- 最大等待 / 最大绕行 / 预约窗口：5 / 8 / 60 分钟。
- 自动 / 人工阈值：82 / 62。
- 权重：0.35 / 0.20 / 0.30 / 0.15。
- 插单策略：`REALTIME_INSERTION`。
- 规则组：启用。

## 最终回归

| 验证 | 结果 |
|---|---|
| 后端 Maven | 209 项；失败 0、错误 0、跳过 24 |
| 前端 Vitest | 31 个测试文件、173 项全部通过 |
| 前端类型检查 | 通过 |
| 前端生产构建 | 通过；141 个模块 |
| 本地模拟路由 | 5/5 通过 |

说明：

- `VehicleLocationCapacityIntegrationTest` 默认跳过，只有显式容量参数才运行 10,000 条写入。
- 详细计划中的模拟路由命令误写为 `apps/route-simulator/tests`；仓库真实入口为 `apps/route-simulator/test_server.py`，按真实入口复跑 5 项全部通过。
- Vite 仍提示单个 648.55 kB JavaScript chunk 超过 500 kB，属于既有非阻断优化项。

## 首日操作边界

- 第一周按 5–10 笔真实订单/日启动。
- 订单只由授权操作员录入，不由脚本生成。
- 只录入已获得授权的真实乘客数据。
- 地址与路线采用“虚拟站点/人工坐标 + 当前估算”。
- ETA 仅作参考，调度员需向乘客和驾驶员明确说明。
- 告警通知只写管理端页面和审计记录。
- 若出现服务不可用、位置无法恢复、派单异常、数据库恢复失败或资源状态不一致，立即停止新增订单。

## 解除阻断步骤

1. 用户在已打开的 `dispatcher02` 首次改密页完成个人密码修改。
2. 改密后通知 Codex。
3. 只读确认 `dispatcher02.mustChangePassword=false`、角色仍仅为 `DISPATCHER`。
4. 复核 API、前端、4 车 4 驾驶员与活动任务状态未变化。
5. 将本记录结论从“阻断”更新为“允许启动首日 5–10 笔真实订单”后，才进入真实订单录入。
