# 待命位置上报与任务追溯本地浏览器验收（2026-08-02）

## 环境

- 管理端：`http://127.0.0.1:5174`
- API：`http://127.0.0.1:18081/actuator/health`，状态 `UP`
- 部署提交：`7bc38ce`
- 验收身份：调度员（具备位置读取、位置上报和任务读取权限）

## 浏览器验收结果

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 待命候选过滤 | 通过 | “待命车辆”下拉框显示 3 辆候选，状态均为 `IDLE`；未显示非空闲候选。 |
| 筛选文案 | 通过 | 显示“任务编号（筛选条件）”，占位符为“输入完整任务编号”。 |
| 位置历史关联展示 | 通过 | 车辆历史同时显示“无任务关联”和“任务 <短编号>”深链。 |
| 隐私边界 | 通过 | 时间线未显示乘客姓名、电话或订单详情。 |
| 任务深链定位 | 通过 | 打开 `/tasks?taskId=cadefc9e-f87a-42e8-878c-45cc26b6a363` 后，对应任务行被选中。 |

## 受控未触发项

- 当前环境存在空闲车，不能在不修改车辆状态的前提下触发“全车忙”空态。
- 修复后的页面不允许选择非空闲车；为避免构造绕过请求或触碰试运行数据，未在真实环境手动发送非空闲车辆的待命上报。
- 上述两项由已通过的自动化回归覆盖：非空闲候选过滤、非空闲无任务 `MANUAL_REPORT` 返回 HTTP 409 且不写事件/快照/审计。

## 结论

本地部署和可无副作用执行的浏览器清单项均通过；受控未触发项已由自动化测试覆盖，未对试运行车辆、订单或位置事件造成写入。
