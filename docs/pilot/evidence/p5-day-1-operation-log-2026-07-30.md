# 首日试运行运营记录（2026-07-30）

## 当前结论

首笔真实订单已录入并自动派单，但最终按“乘客未到”关闭：订单为 `EXCEPTION_CLOSED`，车辆任务为 `CANCELLED`。两个任务站点均未记录实际到达时间，且该任务没有位置事件，因此不能将本单记录为已发车、已到达或已完成。

爽约关闭后资源已正常释放：4/4 车辆 `IDLE`、4/4 驾驶员 `AVAILABLE`、活动任务为 0，可准备下一笔订单。

本记录不包含乘客姓名、电话或精确坐标。

## 运营目标与统计口径

- 日订单节奏：5–10 笔真实订单。
- 自动派单阈值：评分不低于 82。
- 人工复核阈值：评分不低于 62、低于 82。
- 最大预计等待：5 分钟。
- 最大预计绕行：8 分钟。
- 活动任务：`PENDING_DEPARTURE`、`DISPATCHED`、`IN_PROGRESS`、`PAUSED`。
- 异常：不可服务、人工复核、取消、爽约、任务异常、地图降级、重复车辆/驾驶员占用或服务不可用。

## 当日 KPI 快照

| 指标 | 当前值 | 目标/基线 | 状态 |
|---|---:|---:|---|
| 已录入真实订单 | 1 | 5–10 笔/日 | 进行中 |
| 自动派单成功 | 1/1 | 按规则执行 | 通过 |
| 人工复核 | 0 | 仅 62–82 分触发 | 正常 |
| 异常订单 | 1/1 | 0 | 记录并闭环 |
| 爽约订单 | 1/1 | 0 | 记录并闭环 |
| 完成订单 | 0/1 | 持续观察 | 尚无完成样本 |
| 地图降级 | 0 | 0 | 正常 |
| 预计等待 | 1 分钟 | ≤5 分钟 | 通过 |
| 预计绕行 | 3 分钟 | ≤8 分钟 | 通过 |
| 实际等待 | 不可计算 | 完成后记录 | 无到站事件 |
| 全程 ETA 偏差 | 不可计算 | 完成后记录 | 无完成事件 |
| 派单评分 | 85.59 | ≥82 自动派单 | 通过 |
| 候选车辆 | 4 | 当前可用资源 | 已评估 |
| 当前活动任务 | 0 | 无在途订单 | 正常 |
| 空闲车辆 | 4/4 | 资源已释放 | 正常 |

统计时区为 Asia/Shanghai；本快照覆盖首日启动后的第 1 笔真实订单，不与历史模拟验收订单混算。

## 首单记录

| 字段 | 记录 |
|---|---|
| 订单标识 | `7171c50d` |
| 需求类型 | 即时 |
| 人数 | 1 |
| 预计出发 | 12:27 |
| 预计上车 | 12:28 |
| 预计到达 | 12:51:09 |
| 调度结果 | `AUTO_DISPATCH` |
| 派单评分 | 85.59 |
| 预计等待 / 绕行 | 1 / 3 分钟 |
| 地图路由 | `AMAP`，未降级 |
| 车辆任务 | `a7db6bba`，`DISPATCHED` |
| 任务站点 | 2 |
| 审计 | `ORDER_AUTO_DISPATCHED` |

### 最终结果

| 字段 | 记录 |
|---|---|
| 关闭时间 | 12:37:45 |
| 订单终态 | `EXCEPTION_CLOSED` |
| 任务终态 | `CANCELLED` |
| 站点终态 | 2/2 `CANCELLED` |
| 实际到达时间 | 0/2 |
| 位置事件 | 0 |
| 异常审计 | `TASK_CANCELLED_NO_SHOW`、`ORDER_NO_SHOW` |

## 数据质量与异常记录

- 订单、派单决策、车辆任务均为 1 条，任务站点为 2 条，粒度和关联完整。
- 预计出发、预计上车、预计到达时间顺序正确。
- 派单评分、预计等待和预计绕行均满足当前规则。
- 12:37:45 由用户执行“乘客未到”，任务和两个站点联动取消，订单异常关闭。
- 本单没有任务级位置事件，两个站点均没有实际到达时间；实际等待和 ETA 偏差标记为不可计算。
- 异常关闭后 4 辆车辆均为 `IDLE`、4 名驾驶员均为 `AVAILABLE`，资源释放正常。
- 当前业务异常累计 1 条，类型为乘客爽约；没有地图降级、系统错误或重复资源占用。
- 监控口径修正：旧的只读门禁查询未包含 `DISPATCHED`；从本记录起，活动任务统一采用 `PENDING_DEPARTURE`、`DISPATCHED`、`IN_PROGRESS`、`PAUSED`。
- 控制缺口：订单联系方式在系统中仅做非空校验。首单已由用户确认人工核对完成，后续每笔仍需在提交前人工核对。

## 后续监控

1. 下一笔订单已准备空白录入表单，提交前继续人工核对联系方式和授权。
2. 下一任务开始时必须记录 `TASK_STARTED` 位置事件，避免再次出现无法验证发车的情况。
3. 到达上车点时记录实际到达时间，并与预计上车时间比较。
4. 订单完成时记录实际到达时间、全程 ETA 偏差和状态闭环。
5. 当日达到 5–10 笔或停止运营时，汇总完成率、自动派单率、人工介入率、异常率、爽约率和 ETA 偏差。
