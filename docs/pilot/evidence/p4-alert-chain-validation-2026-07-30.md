# P4 告警通知链路验证记录（2026-07-30）

## 结论

通过。`dispatcher02` 在既有 `DISPATCHER` 最小权限下，通过正式 HTTP API 写入一条经服务区包含接口确认的区外位置；接口返回 `OUTSIDE_SERVICE_AREA`。随后立即通过同一正式接口写入已启用区内站点位置，车辆最新快照恢复到区内。`SYSTEM_ADMIN` 在管理端位置历史、审计日志和运营看板完成页面验收。

通知范围严格采用已确认方案：只写管理端页面和审计记录，不发送短信、邮件、即时消息或外部通知。

## 账号与环境边界

| 项目 | 结果 |
|---|---|
| 上报账号 | `dispatcher02` |
| 上报角色 | 仅 `DISPATCHER` |
| 管理端验收账号 | `admin` |
| 管理端验收角色 | `SYSTEM_ADMIN` |
| `dispatcher02` 权限变更 | 无 |
| 订单/任务写入 | 无 |
| API 健康 | `UP` |

账号门禁补充：`dispatcher02.mustChangePassword=true`。这不影响本次既有权限的告警接口验证，但会阻断首日真实订单启动；必须由用户完成首次改密后再解除。

## 只读基线

- 位置上报审计基线：12 条。
- 位置上报候选：4 辆，全部为 `IDLE`。
- 已发布且启用服务区：1 个。
- 已启用虚拟站点：31 个。
- 自然过期位置样本：0 个。

调度工作台只对有活动任务的车辆显示“超过 30 分钟未更新”提示。当前车辆均为 `IDLE` 且没有活动任务，因此该分支按设计记为“不适用”；未伪造任务、未倒填历史时间。

## 正式接口验证

目标车辆：

- 车辆编号：`7150c9ee-5191-49f3-8d1a-691918bb47ca`
- 验证前状态：`IDLE`
- 验证后状态：`IDLE`

包含接口预校验：

| 坐标 | 用途 | 包含结果 |
|---|---|---|
| `105.22813692709364, 35.230171380791923` | 已发布边界外测试点 | `inside=false` |
| `105.258224, 35.197636` | 已启用恢复站点 | `inside=true` |

事件结果：

| 顺序 | 事件编号 | 告警/标记 | 快照 |
|---|---|---|---|
| 1 | `4e95a3cd-7616-4886-9cde-02ee85e2bac4` | `OUTSIDE_SERVICE_AREA`；数据库 `outside_service_area=true` | 已应用 |
| 2 | `42c7403e-4ef5-4148-8a23-f02c85123366` | 无区外告警；数据库 `outside_service_area=false` | 已应用并成为最新快照 |

两次请求均为新事件，`replayed=false`。恢复后车辆最新坐标为 `105.258224, 35.197636`，与启用站点一致。

## 管理端页面验收

使用 `SYSTEM_ADMIN` 现有登录会话完成：

1. 审计日志页筛选 `VEHICLE_LOCATION_REPORTED` 并刷新，显示两条 2026-07-30 11:42:45 新记录：
   - 对象均为 `VEHICLE · 7150c9ee`；
   - 操作者均为 `dispatcher02`；
   - 动作均为 `VEHICLE_LOCATION_REPORTED`。
2. 位置历史页按完整车辆编号查询，按时间显示：
   - `P4 outside service area test point`；
   - 紧随其后的 `P4 recovery enabled stop`。
3. `SYSTEM_ADMIN` 运营看板正常加载，指标页面无授权或加载错误。

## 审计与一致性

| 核对项 | 结果 |
|---|---|
| 位置上报审计基线 | 12 |
| 验证后位置上报审计 | 14 |
| 审计增量 | 2 |
| 4 辆车状态 | `IDLE=4` |
| 4 名驾驶员状态 | `AVAILABLE=4` |
| 活动任务 | 0 |
| 同车重复活动任务 | 0 |
| 最新车辆快照 | 指向区内恢复事件 |

## 后续

告警通知链路 P0 阻断项解除。三项 P0 技术验证均已通过；首日启动门禁继续检查账号首次改密、服务健康、资源与规则基线和最终回归。
