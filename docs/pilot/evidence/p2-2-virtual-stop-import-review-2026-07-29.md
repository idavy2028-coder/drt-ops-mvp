# P2-2 虚拟站点导入与地图复核证据

**执行日期：** 2026-07-29（Asia/Shanghai）
**执行角色：** 资源配置管理员、开发核验（Codex）
**代码基线：** `codex/p1-vehicle-location-calibration`，`439228bb45e1f9a9c922f794d626e7f5561d30e0`
**数据环境：** 本机管理端 `127.0.0.1:5174`、API `127.0.0.1:18081`、隔离 PostGIS 数据库 `drt_ops_pilot_bootstrap`

## 执行边界

- 已复用用户登录的资源配置管理员会话，未读取或记录密码、令牌等认证信息。
- 在接管页面时，数据库和页面已经存在本批 10 个新增站点；文件选择框为空，授权工作区内也未发现本批原始 CSV。
- 因原始 CSV 不可再读取，本次无法对源文件逐行重放离线预校验；改为对已持久化记录、导入审计、重复导入结果和地图空间关系进行等价校验。
- 审计记录已经证明同一批次首次导入创建 10 条、再次提交跳过 10 条。为避免无意义的第三次提交，本次没有再次点击导入。
- 本次没有修改站点数据，没有发布服务区草稿 v6。

## 导入审计

| 时间（Asia/Shanghai） | 动作 | 创建数 | 跳过数 | 结论 |
| --- | --- | ---: | ---: | --- |
| 2026-07-29 18:17:11 | `VIRTUAL_STOP_IMPORTED` | 10 | 0 | 本批 10 个站点已成功创建 |
| 2026-07-29 18:58:44 | `VIRTUAL_STOP_IMPORTED` | 0 | 10 | 同批重复提交全部跳过，未生成重复站点 |

本批新增站点为：一中东门、公安局、文庙街小学、农业局家属院、文化广场、工商银行、国税局、财政局、工信局、县政府。

## 持久化数据预校验

| 校验项 | 结果 | 状态 |
| --- | --- | --- |
| 站点总数 | 31，达到计划要求的 30 至 50 个 | 通过 |
| 名称与地址 | 31/31 非空 | 通过 |
| 坐标系 | 31/31 为 `GCJ-02` | 通过 |
| 服务半径 | 31/31 为正数；本批 10 个均为 500 米 | 通过 |
| 上下车能力 | 本批 10 个均允许上车和下车 | 通过 |
| 核验时间 | 31/31 已记录 | 通过 |
| 名称去重 | 规范化名称无重复 | 通过 |
| 启停状态 | 启用 26、停用 5 | 通过，停用项转 P2-3 |
| 安全说明 | 当前记录仍使用“示例数据，需现场核验” | 待 P2-3 复核 |

## 服务区关系与地图复核

PostGIS 按系统一致的 `ST_Covers` 口径核验：

| 边界 | 状态 | 几何 | 区内 | 区外 |
| --- | --- | --- | ---: | ---: |
| 发布版本 v5 | 已发布、已启用 | 有效 | 26 | 5 |
| 草稿版本 v6 | 未发布 | 有效、与 v5 不相同 | 31 | 0 |

真实浏览器资源配置页显示 31 个虚拟站点、启用 26 个、停用 5 个；地图在草稿 v6 预览下显示区内 31、区外 0。草稿 v6 带有“草稿待发布”状态，本次未点击“发布并启用”。

位于已发布 v5 边界外且保持停用的 5 个站点：

| 站点 | 距已发布 v5 边界约 | 当前状态 |
| --- | ---: | --- |
| 工商银行 | 115.9 米 | 停用 |
| 国税局 | 208.2 米 | 停用 |
| 财政局 | 273.9 米 | 停用 |
| 工信局 | 390.2 米 | 停用 |
| 县政府 | 537.6 米 | 停用 |

其余本批 5 个新增站点位于已发布 v5 边界内并已启用：一中东门、公安局、文庙街小学、农业局家属院、文化广场。

## 风险与后续约束

1. 原始 CSV 未保留在授权工作区，页面文件选择框也为空，因此源文件级预校验缺少可复核附件；本次结论基于持久化数据与审计链。
2. 全部站点的安全说明仍为占位文字。P2-3 在形成启用清单前，应由运营人员补充或确认真实上下车安全说明。
3. P2-1 已确认的是发布版本 v5。草稿 v6 虽覆盖 31 个站点，但扩大了运营范围，未经新的业务边界确认不得发布。
4. 5 个 v5 区外站点当前停用，不会作为已发布服务区内的可用站点；P2-3 应将其列为“暂不启用”或“待复核”，并明确是调整站点坐标还是另行审批边界 v6。

## 阶段结论

P2-2 的计划完成标准已经满足：站点总数达到 31 个，必填字段、坐标系、服务半径、上下车能力、核验状态、启停状态和重复导入行为均已核验，地图与服务区关系已明确。

P2-2 状态标记为完成。质量跟进转入 P2-3：复核安全说明，形成“启用、暂不启用、待复核”清单，并处理 5 个发布边界外站点；在获得新的业务边界确认前保持草稿 v6 未发布。
