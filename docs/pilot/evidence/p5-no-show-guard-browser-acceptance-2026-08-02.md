# P5“乘客未到”防误操作真实浏览器验收（2026-08-02）

## 验收结论

当前本地试点环境已完成真实浏览器验收，5 分钟门禁、二次确认、订单/任务/站点联动和审计记录均通过。验收订单仅用于本次防误操作流程复核，不计入 2026-07-30 首日真实业务订单 KPI。

## 验收对象

- 订单标识：`81674d3c`
- 任务标识：`cb302c9e`
- 车辆标识：`7150c9ee-5191-49f3-8d1a-691918bb47ca`
- 上车点：高铁站
- 操作账号：`dispatcher02`
- 管理端复核账号：`admin`（`SYSTEM_ADMIN`）

## 浏览器验收过程

1. 任务发车后、车辆未到达上车点时，订单页显示“车辆尚未到达上车点”，不能执行“乘客未到”。
2. 车辆于 08:29:13 到达上车点后，页面显示 5 分钟倒计时；抽样观察到 4 分 40 秒、3 分 32 秒、2 分 30 秒、1 分 26 秒和 24 秒，倒计时期间按钮不可用。
3. 等候期结束后，“乘客未到”按钮才可用。二次确认对话框展示订单、计划上车时间、最早处理时间、有效等候时长和联动影响；未选择固定原因时确认按钮不可用。
4. 选择固定原因“乘客在等待期内未出现”后提交成功。订单终态为 `EXCEPTION_CLOSED`，任务终态为 `CANCELLED`，上车和下车两个任务站点均为 `CANCELLED`。

## 管理端与审计复核

- 车辆任务页确认任务 `cb302c9e` 已取消，两个站点均已取消。
- 资源配置页显示 4 辆车中 3 辆空闲、1 辆待发车；该待发车车辆属于既存任务 `4e526376`，不是本次验收任务。本次车辆对应任务已取消，不存在重复活动任务占用。
- 驾驶员资源显示 3 人可用、1 人忙碌；忙碌状态与上述既存待发车任务一致，不是本次验收遗留。
- 审计页复核到以下记录：
  - 08:28:28：任务 `cb302c9e`，车辆发车；
  - 08:29:13：任务 `cb302c9e`，车辆到站；
  - 08:35:06：任务 `cb302c9e`，`TASK_CANCELLED_NO_SHOW`；
  - 08:35:06：订单 `81674d3c`，乘客未到关闭。
- 两条关闭审计的操作者均为 `dispatcher02`，原因均为“乘客在等待期内未出现”。

## 发现的非阻断问题

- 任务级“发车”对话框如果选择虚拟站点，会携带站点标识并被服务端以“当前任务动作没有可关联的任务节点”拒绝；切换为人工坐标后可正常发车。该问题不影响本次“乘客未到”门禁验收，但应作为任务动作表单默认值缺陷单独修复。
- 运营看板按运营日聚合。该订单计划运营日为 2026-07-30、实际恢复执行日为 2026-08-02，因此 8 月 2 日“今日”卡片为 0，不能用作本次跨日验收的单笔指标证据；本次以任务页、资源页和审计页交叉复核。

## 最终判定

P5“乘客未到”防误操作整改在当前本地试点环境的真实浏览器验收通过，可以继续用于后续真实订单试运行。既存待发车任务 `4e526376` 需按原业务流程单独处置，不归因于本次整改。
