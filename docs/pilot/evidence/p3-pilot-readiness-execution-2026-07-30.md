# P3 试点运行准备执行记录

**执行日期：** 2026-07-30（Asia/Shanghai）  
**代码基线：** `codex/p1-vehicle-location-calibration`，`439228bb45e1f9a9c922f794d626e7f5561d30e0`  
**数据环境：** 本机管理端 `127.0.0.1:5174`、API `127.0.0.1:18081`、隔离 PostGIS 数据库 `drt_ops_pilot_bootstrap`  
**执行账号：** `dispatcher02`（`DISPATCHER`）  
**数据口径：** 用户确认使用模拟验收数据；模拟联系方式统一使用非真实号码，所有反馈备注均明确标记“P3 模拟验收”。

## 执行结论

P3 已于 2026-07-30 完成修复复验并正式收口：

- P3-1 即时需求录入：完成。
- P3-2 调度与人工复核：人工复核路径和本地模拟路由下的自动派单路径均已完成。
- P3-3 电话/微信下达与反馈台账：以“非真实通联”的模拟反馈完成，驾驶员反馈时间与系统录入时间分开保存。
- P3-4 任务节点和位置事件主链：完成。
- P3-5 取消、爽约、车辆故障、严重延误：四类动作及审计均已完成；爽约任务联动缺陷已修复并复验。
- P3-6 跨页面、审计和看板一致性：已使用 `SYSTEM_ADMIN` 完成页面验收，最小权限方案 A 保持不变。

## P3 启动前清场

用户确认旧 3 笔人工复核订单统一拒绝。通过调度页面使用统一原因“P3 验收前清理历史测试样本”完成拒绝：

| 订单 | 最终状态 | 拒绝审计时间 |
| --- | --- | --- |
| `77d6d309-fd3f-47b0-ae77-07b33d19830b` | `UNSERVICEABLE` | 07:26:59 |
| `cfec2569-a9c8-42cd-8b8e-2845ba46b2c3` | `UNSERVICEABLE` | 07:28:26 |
| `7761fa69-98f1-4518-860c-3361fc85716d` | `UNSERVICEABLE` | 07:28:37 |

清场后待人工复核订单为 0，车辆任务为 0；三条 `MANUAL_REVIEW_REJECTED` 审计均由 `dispatcher02` 对应用户执行。

## P3-1 即时需求录入

模拟订单：

- 订单：`dc6a080d-e8a9-4e66-ba4c-9dacf0e0b2c9`
- 乘客：`P3-1模拟验收乘客A`
- 人数：1
- 类型：`IMMEDIATE`
- 起点：高铁站，`105.258224, 35.197636`
- 终点：县政府，`105.238159, 35.211498`
- 预计出发：2026-07-30 07:48:00

页面在提交前分别提示起点、终点位于服务区内，并自动推荐高铁站和县政府。数据库中的站点外键、坐标、需求类型和页面一致，创建后状态为 `PENDING_DISPATCH`。

## P3-2 调度与人工复核

订单 `dc6a080d…` 调度结果：

- 决策：`6ef4a8c5-8394-4048-8031-f01b5ced07e6`
- 候选车辆：4
- 最佳车辆：`af0d7785-4606-4451-b451-6befa043890a`（甘J16396D）
- 分数：85.59，高于自动阈值 82
- 预计等候：1 分钟
- 预计绕行：3 分钟
- 决策结果：`MANUAL_REVIEW`
- 人工复核原因：`MAP_ROUTE_UNAVAILABLE`

地图路径服务未启用，路径估算降级为 `STRAIGHT_LINE`。即使分数达到自动阈值，系统仍强制人工复核，因此当前环境不能产生符合验收口径的自动派单样本。

人工复核由 `dispatcher02` 于 07:36:53 确认，生成任务：

- 任务：`0ecc7ce6-c6cb-451e-8583-e046d83bdef7`
- 车辆：甘J16396D
- 驾驶员：苟毅春
- 来源：`MANUAL_REVIEW`
- 审计：`ORDER_PENDING_MANUAL_REVIEW`、`MANUAL_REVIEW_APPROVED`

## P3-3 模拟下达与反馈台账

本次没有发生真实电话或微信通联，以下时间全部按用户授权的模拟验收口径记录：

| 任务 | 下达时间 | 模拟驾驶员反馈时间 | 调度员系统录入时间 | 说明 |
| --- | --- | --- | --- | --- |
| `0ecc7ce6…` | 07:36:53 | 07:37:00 | 07:39:41 | 备注为“电话反馈已收到（非真实通联）” |

位置历史页面分别展示“驾驶员反馈 07:37”和“系统录入 07:39”，录入延迟显示 3 分钟。该人工录入间隔未混入算法预计等候时间；算法预计等候仍为 1 分钟。

## P3-4 任务节点与位置事件链

任务 `0ecc7ce6…` 已完成以下完整链路：

| 事件 | 位置 | 驾驶员反馈时间 | 系统录入时间 | 审计动作 |
| --- | --- | --- | --- | --- |
| `TASK_STARTED` | 高铁站 | 07:37:00 | 07:39:41 | `TASK_STARTED` |
| `PICKUP_ARRIVED` | 高铁站 | 07:39:00 | 07:40:03 | `TASK_STOP_ARRIVED` |
| `PASSENGER_BOARDED` | 高铁站 | 07:40:00 | 07:40:15 | `PASSENGER_BOARDED` |
| `DROPOFF_ARRIVED` | 县政府 | 07:40:00 | 07:40:40 | `TASK_STOP_ARRIVED` |
| `PASSENGER_ALIGHTED` | 县政府 | 07:40:00 | 07:40:53 | `PASSENGER_ALIGHTED` |
| `TASK_COMPLETED` | 县政府 | 07:41:00 | 07:41:13 | `TASK_COMPLETED` |

两个任务节点最终分别为 `BOARDED`、`ALIGHTED`，任务与订单均为 `COMPLETED`。6 个位置事件全部应用到最新位置快照，均位于服务区内，审计元数据中的位置事件 ID 与事件表一一对应。

任务完成弹窗默认带入县政府的 `virtualStopId`，但后端对任务级“完成”动作禁止携带任务节点。验收时在页面切换为“手工录入”，保留县政府坐标和地址后完成。该默认值冲突属于界面缺陷。

## P3-5 异常演练

| 场景 | 订单 | 订单状态 | 任务 | 任务状态 | 审计 |
| --- | --- | --- | --- | --- | --- |
| 取消 | `3779b93b…` | `CANCELLED` | 无 | 无 | `ORDER_CANCELLED` |
| 乘客未到 | `9f9bec16…` | `EXCEPTION_CLOSED` | `f41f370b…` | `DISPATCHED` | `ORDER_NO_SHOW` |
| 车辆故障 | `6230ee65…` | `EXCEPTION_CLOSED` | `178b2307…` | `EXCEPTION` | `TASK_EXCEPTION` |
| 严重延误 | `e82d5e69…` | `EXCEPTION_CLOSED` | `a713124d…` | `EXCEPTION` | `TASK_SEVERE_DELAY` |

取消原因是“运营后台取消”，爽约原因是“乘客未上车”，故障原因是“车辆故障”，延误原因是“预计到达严重延误”。四类异常动作均由 `dispatcher02` 对应用户执行并保留审计。

## P3-6 一致性复核

已完成的页面核对：

- 订单页显示主链订单已完成，取消单已取消，爽约/故障/延误订单均异常关闭。
- 任务页显示主链任务已完成，故障和延误任务为异常任务。
- 位置历史页显示主链 6 个事件，并分别展示驾驶员反馈时间、系统录入时间和录入延迟。
- 工作台显示待调度 0、待复核 0、车辆任务 4，并显示主链完整人工节点链。

首次验收未通过项（现已全部修复并复验）：

1. 爽约订单 `9f9bec16…` 已异常关闭，但任务 `f41f370b…` 仍为 `DISPATCHED`，订单与任务状态不一致。
2. 主链、爽约、故障、延误 4 个任务全部分配给甘J16396D；车辆仍显示 `IDLE`、可调度，系统没有阻止同一车辆持有多个未结束或异常任务。
3. 自动派单率实际为 0；地图路径降级导致 P3-2 自动派单样本缺失。
4. `DISPATCHER` 角色不具备 `AUDIT_READ` 和 `METRICS_READ`，因此不能用 `dispatcher02` 完成审计页和运营看板页面级复核；本记录仅通过只读数据库完成审计核对。
5. `dispatcher02` 数据库仍标记 `must_change_password=true`，但临时凭据登录后未强制跳转改密。
6. 已完成主链订单 `dc6a080d…` 仍保留 `failure_reason=MAP_ROUTE_UNAVAILABLE`；状态闭环正确，但失败原因字段未随人工确认和任务完成清除。

## 首次验收收口条件（现已全部满足）

P3 收口前至少需要：

1. 启用真实地图路由，或由业务负责人明确批准用于验收的本地模拟路由，并完成至少 1 笔自动派单。
2. 明确并修复爽约订单关闭时的任务联动规则。
3. 修复车辆任务占用/可调度状态，使同一车辆不能被重复分配到冲突任务。
4. 修复任务完成弹窗默认携带 `virtualStopId` 的问题。
5. 使用具备 `AUDIT_READ`、`METRICS_READ` 的主管/审计账号完成审计页和运营看板页面复核。
6. 复核首次登录强制改密机制。
7. 明确人工复核后订单失败原因字段的保留或清理规则。

## P3-6 修复复验（方案 A）

用户确认采用最小权限方案 A：`dispatcher02` 继续仅保留 `DISPATCHER` 角色，审计页和运营看板改由 `SYSTEM_ADMIN` 账号验收；同时批准本地模拟路由用于自动派单验收。

### 修复内容

- 增加本地模拟路由服务，仅用于试点验收环境；API 仍按地图路径提供方接口调用并记录路径提供方、距离、时长与降级状态。
- 订单从人工复核或失败状态转为确认时清除历史 `failure_reason`。
- 车辆和驾驶员随任务创建、发车、完成、异常关闭与取消同步进入占用或释放状态；候选组装排除已有活动任务的空闲车辆，并使用数据库行锁保护资源分配。
- 单订单任务发生爽约时，任务和任务节点同步取消并释放车辆、驾驶员；共享任务仅取消对应订单节点，不影响其他订单。
- 任务完成弹窗不再向任务级“完成”动作携带 `virtualStopId`。
- `must_change_password=true` 的账号登录后强制进入改密页；路由守卫阻止在改密完成前访问其他业务页面。
- 修复真实算法协议回传“新任务候选合成 taskId”时被误判为既有任务的 409：只有与本次车辆匹配的合成 ID 走新建任务分支，失效或随机的既有任务 ID 仍返回冲突。

### 自动派单与完整任务链

- 模拟订单：`b41b9827-c0ef-4278-b7b4-2cc0bfde2a31`
- 自动派单决策：`e1e18df2-2a15-40a6-a856-345de0bfe1f5`
- 任务：`24b22cce-d1b2-432c-a8c8-3d1333bbea82`
- 决策结果：`AUTO_DISPATCH`
- 地图记录：`AMAP`、`map_degraded=false`
- 派单后订单为 `CONFIRMED` 且 `failure_reason` 为空；任务为 `DISPATCHED`，恰好一车为 `DISPATCHED`、一名驾驶员为 `BUSY`，不存在同车多个活动任务。
- 已依次完成 `TASK_STARTED`、`PICKUP_ARRIVED`、`PASSENGER_BOARDED`、`DROPOFF_ARRIVED`、`PASSENGER_ALIGHTED`、`TASK_COMPLETED` 六个事件。
- 最终订单和任务均为 `COMPLETED`，两站分别为 `BOARDED`、`ALIGHTED`；位置事件 6 条、任务审计 6 条；4 车全部 `IDLE`、4 名驾驶员全部 `AVAILABLE`。

### 爽约联动复验

- 一笔得分 `81.39`、未达自动阈值 `82` 的模拟订单按规则进入人工复核，已由调度员正常拒绝，没有绕过阈值强制派单。
- 替代模拟订单：`2aa9bd81-1304-46f8-8bd0-7fc09ffb2fe4`
- 自动派单决策：`d3b6f39e-2e50-4df8-a396-b8a271ac0cff`
- 任务：`775d2d43-c314-4fec-96d6-c83c2dd8cfa9`
- 爽约后订单为 `EXCEPTION_CLOSED`，原因“P3-6模拟验收：乘客未按约到达”已持久化；任务及两个节点均为 `CANCELLED`。
- 审计包含 `ORDER_AUTO_DISPATCHED`、`TASK_CANCELLED_NO_SHOW`、`ORDER_NO_SHOW`；最终 4 车全部 `IDLE`、4 名驾驶员全部 `AVAILABLE`，不存在同车多个活动任务。

### 账号与页面验收状态

- `dispatcher02` 登录后已在真实前端强制跳转改密页；该账号仍仅有 `DISPATCHER` 角色，未增加 `AUDIT_READ` 或 `METRICS_READ`。
- 用户已亲自完成 `SYSTEM_ADMIN` 首次改密；数据库核对 `admin` 已启用、`must_change_password=false`，角色为 `SYSTEM_ADMIN`。
- `SYSTEM_ADMIN` 会话可直接进入运营看板和审计日志，不再跳转改密页。
- 运营看板显示订单量 8、订单确认率 25%、自动派发率 25%、人工复核率 63%、平均等待 1.7 分钟、平均绕行 3.0 分钟、任务完成率 33%、异常关闭率 50%、车辆利用率 100%。
- 数据库按 `Asia/Shanghai` 运营日和服务端同一口径复算：订单 8、确认口径 2、自动派单 2、人工复核 5、平均等待 1.71 分钟、平均绕行 3.00 分钟、任务 6、完成 2、异常关闭订单 4、利用车辆 1/任务车辆 1；与页面显示一致。
- 审计日志页可查看 P3-6 自动派单、任务完整执行、爽约任务取消和订单爽约记录；按操作筛选 `ORDER_AUTO_DISPATCHED` 精确返回订单 `b41b9827…`、`2aa9bd81…` 两条记录。

### 最终自动化验证

- 后端 Surefire：57 个报告、208 项测试，失败 0、错误 0、跳过 23。
- 前端 Vitest：31 个测试文件、173 项测试全部通过；`vue-tsc --noEmit` 和生产构建通过。构建保留单个 648.55 kB chunk 警告，不阻断本阶段验收。
- 本地模拟路由：5 项单元测试通过；运行时路线探测返回状态 `1`、距离 1401 米、时长 145 秒。
- `docker compose -f infra/docker-compose.pilot.yml config --quiet` 通过；API 健康状态为 `UP`。
- `git diff --check` 通过。

P3-6 状态：已通过。P3 阶段状态：已完成并收口。
