# MVP 就绪检查清单

检查日期：2026-07-09

本清单用于判断“区域动态响应公交运营管理 MVP”是否具备企业内部演示和第一轮业务验收条件。结论先行：当前版本已具备本地演示闭环，不等同于生产上线就绪。

## 一、核心范围

| 检查项 | 当前状态 | 说明 |
| --- | --- | --- |
| 服务区配置 | 已覆盖 | 已有服务区 API、页面入口、演示数据和测试。 |
| 虚拟站点配置 | 已覆盖 | 支持上车/下车属性、服务半径、启停状态和最近站点匹配。 |
| 车辆配置 | 已覆盖 | 支持车辆基础资料、容量、调度状态和是否可派。 |
| 司机配置 | 已覆盖 | 支持资质、班次时间、当前状态和车队归属。 |
| 调度规则组 | 已覆盖 | 支持等待、绕行、自动派发阈值、人工复核阈值和权重配置。 |
| 即时需求录入 | 已覆盖 | 管理端可录入乘客需求并调用后端创建订单。 |
| 短时预约订单 | 基础覆盖 | 数据模型和表单包含 `RESERVATION` 类型，预约策略仍需后续细化。 |
| 虚拟站点匹配 | 已覆盖 | 订单创建时自动匹配上车点和下车点。 |
| 自动派发 | 已覆盖 | 后端编排调用算法结果，确认订单并生成车辆任务。 |
| 人工复核 | 已覆盖 | 支持人工批准/拒绝调度决策。 |
| 无可行方案 | 已覆盖 | 算法和订单状态支持不可服务结果。 |
| 车辆任务创建 | 已覆盖 | 自动派发后生成任务和上下车站点。 |
| 车辆任务执行 | 已覆盖 | 支持发车、到站、上车、下车、完成。 |
| 动态插单 | 基础覆盖 | 支持自动派发和人工复核把新订单插入已派发或执行中的可派任务；多位置重排和全局最优仍需后续增强。 |
| 订单取消 | 已覆盖 | 支持运营后台取消并记录原因。 |
| 乘客未到 | 已覆盖 | 支持未到异常闭环。 |
| 车辆故障 | 已覆盖 | 支持任务异常并关闭关联订单。 |
| 严重延误 | 已覆盖 | 支持将严重延误任务异常关闭、同步关闭关联订单并记录独立审计动作。 |
| 审计日志 | 已覆盖 | 覆盖调度决策、人工操作、任务状态变化和异常处理。 |
| 指标看板 | 已覆盖 | 覆盖订单量、确认率、自动派发率、人工复核率、等待、绕行、完成率、异常关闭率和车辆利用率。 |
| 调度工作台 | 已覆盖 | 包含实时订单、车辆任务、地图占位、算法解释和调度摘要。 |
| 资源管理页面 | 已覆盖 | 包含服务区、虚拟站点、车辆、司机。 |
| 任务模拟页面 | 已覆盖 | 已接入任务列表和执行动作接口。 |

## 二、验证结果

已在本地完成以下验证：

```powershell
mvn -q -pl apps/api test
python -m pytest -v
cd apps/admin-web
npm audit --json
npm run typecheck
npm run test
npm run build
npm run e2e -- dispatch-flow.spec.ts
```

验证结论：

- 后端全量测试通过，包含 `DispatchFlowIntegrationTest` 的需求录入、调度、任务执行、订单完成闭环。
- 算法测试通过，覆盖无车不可服务、同向低绕行自动派发、反向任务拒绝和健康检查。
- 前端类型检查、单元测试、生产构建和 Playwright 演示流程通过，`npm run e2e -- dispatch-flow.spec.ts` 可正常退出。
- 前端依赖已升级，`npm audit --json` 当前报告 0 个漏洞。
- Playwright 流程使用接口 mock 验证管理端页面操作和接口连线；完整全栈联调仍建议按 README 启动 Postgres、API、算法和前端后人工演示。

## 三、已知风险

- Vite 构建提示 MapLibre chunk 超过 500 kB，当前可接受，后续可做地图模块拆包优化。
- PostGIS 迁移集成测试需 Docker/PostGIS 环境；常规 H2 测试不等同于空间数据库生产验证。
- 复杂动态插单重排、权限角色、生产级消息队列、完整司机端、完整乘客端、监管端、支付结算不在本 MVP 范围内。

## 四、就绪结论

当前版本适合进入公交企业内部 MVP 演示和业务验收，演示主线为：

1. 配置服务区、虚拟站点、车辆、司机和调度规则。
2. 在订单中心录入乘客需求。
3. 触发调度，查看自动派发或人工复核结果。
4. 在任务页面模拟车辆执行。
5. 在调度工作台、看板和审计日志查看运营闭环结果。

进入生产试点前，需要补齐权限体系、真实地图/路径服务、全栈环境压测、PostGIS 环境验证、持续安全依赖监控和运维监控。
